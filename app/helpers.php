<?php

/*
 * Global helpers file with misc functions.
 */

if (!function_exists('generate_breadcrumb')) {
    /**
     * Helper to grab the application name.
     *
     * @return mixed
     */
    function generate_breadcrumb($breadcrumb)
    {
        return $breadcrumb;
    }
}
